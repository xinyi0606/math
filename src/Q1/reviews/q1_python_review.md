# Q1 Python代码审查

> 审查结论：PASS
> 对象：`src/Q1/run_q1.py`、`src/common/optimization.py`、`src/common/reporting.py`

## 具体通过项

1. **PASS—量纲**：`run_q1.py:42`先将kW乘 `DT_HOURS=1/6` 转为kWh，费用才与元/kWh相乘。
2. **PASS—目标函数**：`optimization.py:71-75`仅给购电变量配置价格系数，充放电不被错误重复收费。
3. **PASS—储能状态**：`optimization.py:39-42,157`使用 $E_{t+1}=E_t+0.9c_t-d_t/0.9$，上下界方向正确。
4. **PASS—终端约束**：`run_q1.py:46`显式传入6000 kWh，`optimization.py:134-139`以累计净充电量等式实现。
5. **PASS—互斥风险**：`run_q1.py:49-51`逐时审计，任何同时充放电都会中止而非静默生成结果。
6. **PASS—独立交叉验证**：`run_q1.py:19-35,53-57`在同一10分钟数据上逐步加密SOC网格，没有继续沿用不可比的小时PoC。
7. **PASS—模板**：`reporting.py:47-60`只填官方工作簿既有单元格，不重命名或重排工作表。

## 约束方向核对

| 约束 | 代码形式 | 方向 | 结论 |
|---|---|---|---|
| 供电平衡 | `-g + c - d <= -net` | 等价于 `g+d-c >= net` | PASS |
| SOC上界 | `0.9 Lc - Ld/0.9 <= 10800-E0` | 上界 | PASS |
| SOC下界 | `-0.9 Lc + Ld/0.9 <= E0-1200` | 下界 | PASS |
| 充放电功率 | `0 <= c,d <= 5000/6` | 双边 | PASS |
| 日末状态 | `0.9 sum(c)-sum(d)/0.9=0` | 等式 | PASS |

## 结果复核

LP费用35126.95元，最大平衡残差 $1.14\times10^{-13}$ kWh，终端误差 $6.37\times10^{-12}$ kWh，同时充放电为0。50 kWh DP网格相对LP差0.79%，方向与连续松弛理论一致。

## 风险与处置

LP未显式使用二元互斥变量，但实际解通过逐时互斥审计；如果后续敏感性出现同时充放电，必须按Q1-D01升级MILP。当前没有阻塞Q1首轮结果的代码问题。
