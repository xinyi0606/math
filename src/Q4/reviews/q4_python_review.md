# Q4 Python代码审查

> 审查结论：PASS / ROUND2_GENERATED / Q3_STABILITY_PENDING
> 对象：`src/Q4/run_q4.py`、`src/common/forecasting.py`、`src/common/optimization.py`

## 具体通过项

1. **PASS—价格信息分层**：`run_q4.py:84-96`将M2显式分支为当天真实价格oracle，将M3分支为历史情景，不混用两者结论。
2. **PASS—无未来情景**：`forecasting.py:95-104`候选日期为`np.arange(day_index)`，因此情景索引严格小于目标日。
3. **PASS—滚动条件化**：发布时刻之后只用当天已实现价格前缀计算与历史日的距离，返回的仍是历史日未来后缀。
4. **PASS—真正min-max目标**：`optimization.py:77-84,121-130`用上境变量约束每个情景费用，不以简单平均或逐点最高价冒充情景最坏费用。
5. **PASS—Q4最小变更**：Q4复用Q2净负荷预测、Q3光伏裕度、相同物理内核和结算函数，仅替换价格决策层。
6. **PASS—泄漏指标**：M3在Q4-2和Q4-3的未来价格泄漏计数均为0；M2的334次被保留并明确解释为oracle定义。
7. **PASS—官方输出**：`result4-2.xlsx`和`result4-3.xlsx`保持模板工作表与行列结构，文件分别约0.36 MB和0.78 MB。

## 约束方向核对

| 约束 | 代码形式 | 方向 | 结论 |
|---|---|---|---|
| 情景最坏费用 | `price_s @ g - z <= 0` | `z >= C_s` | PASS |
| 调整情景费用 | `sum(u_s)-z <= 0` | `z >= C_s^A` | PASS |
| 历史情景日期 | `indices < day_index` | 严格过去 | PASS |
| 滚动前缀 | 只在`start_slot`前计算距离 | 不读未来后缀 | PASS |
| 实际结算 | 求解后用当天已实现价格点乘 | 事后评价 | PASS |
| oracle隔离 | M2单独标记并统计334次前视 | 不作可实施结论 | PASS |

## 结果复核

Q4-2的M3总费用17786226.74元，相对oracle机会损失0.64%；Q4-3为18600451.92元，机会损失0.68%。两类M3未来价格泄漏均为0、全年末SOC为6000 kWh、同时充放电为0。

## Round2 参数传播复审（2026-09-12）

1. **PASS—显式参数接口**：`run_q4.py:263-275`将round编号和Q3安全裕度显式传入Q4-3模拟。
2. **PASS—四次发布一致**：`run_q4.py:156,181-182`在00:00、06:00、12:00、18:00均使用同一0.99分位参数。
3. **PASS—目录隔离**：`run_q4.py:263-265`将round2写入独立实验目录，保留round1证据。
4. **PASS—追溯字段**：`run_q4.py:324-332`在指标和运行摘要中记录 `reserve_alpha_q4_3=0.99`。
5. **PASS—非前视保持**：round2 Q4-3 M3未来价格泄漏次数为0；M2的334次继续仅作为oracle标记。
6. **PASS—物理约束**：Q4-3 M3年末SOC为6000 kWh，同时充放电为0，最大平衡残差低于 $2\times10^{-11}$ kWh。
7. **PASS—正式输出**：round2已生成Q4-2/Q4-3表格、逐时压缩表、JSON、日志、比较图及两个官方工作簿。

### 已修复问题

- `output/result4-3.xlsx` 的调整购电量曾出现一个 `-2.27e-12 kWh` 的求解器浮点残差。`src/common/reporting.py` 现将容差内负零规范化为0，并对小于 `-1e-6` 的实质负数直接报错，避免掩盖真实不可行解。

## 依赖限制

Q4-3已继承Q3的0.99分位配置完成重算。Q4价格情景方法通过代码审查，但Q3稳定性置信度与结果判定仍为PENDING，因此当前工作簿不得标为冻结结果。
