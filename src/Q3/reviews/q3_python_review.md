# Q3 Python代码审查

> 审查结论：CODE_PASS / ROUND2_GENERATED / STABILITY_VERDICT_PENDING
> 对象：`src/Q3/run_q3.py`、`src/common/data.py`、`src/common/optimization.py`

## 具体通过项

1. **PASS—预测映射**：`data.py:94-109`按发布时刻+小时节点插值到冻结的10分钟列序；更新前缀使用已实现值，未从未来实际光伏取边界。
2. **PASS—负荷口径**：`run_q3.py:66-71`由Q2净负荷预测加00:00光伏预测恢复日前负荷，再替换最新光伏，避免把实际负荷偷渡进决策。
3. **PASS—历史分位**：`forecasting.py:73-83`只截取 `[:day_index]` 的历史误差；修改目标日及未来预测不会改变当日裕度。
4. **PASS—冻结前缀**：`run_q3.py:85-106`每次只把`start:`后缀替换，已执行的购电与储能动作不回改。
5. **PASS—调整量语义**：`run_q3.py:102-106,117-129`保存调整后的总购电量，并最终只相对原始`plan_grid`结算一次。
6. **PASS—非对称费用**：`optimization.py:191-208`下调按50%退款，上调超出部分按1.5倍收费，紧急量另按5倍结算。
7. **PASS—反向证据保留**：代码输出M3不利结果和五档分位敏感性，没有按预期方向筛选或删除候选。

## 约束方向核对

| 约束 | 代码形式 | 方向 | 结论 |
|---|---|---|---|
| 更新后供电 | `gA+d-c >= load_hat-pv_safe` | 不低于预测需求 | PASS |
| 冻结历史 | 仅赋值数组`start:` | 过去不可改 | PASS |
| 下调结算 | `-0.5*p*max(gP-gA,0)` | 退款 | PASS |
| 上调结算 | `+1.5*p*max(gA-gP,0)` | 溢价 | PASS |
| 实际紧急量 | `max(actual_net+c-d-gA,0)` | 补足正缺口 | PASS |
| 年末SOC | 最后一次滚动仍传入6000 | 等式 | PASS |

## 结果复核

0.90方案覆盖率86.12%，总费用17627775.98元、紧急购电1186291.50 kWh，均未达到相对M1的最终保留条件。0.99敏感性使紧急量较M1下降17.50%，代价为总费用增加3.58%。代码与约束审查通过，但正式参数需要新的建模者决定或独立验证，当前Q3不应进入结果冻结。

## Round2 参数化改动复审（2026-09-12）

1. **PASS—目录隔离**：`run_q3.py:159-161`将轮次作为显式参数传入 `round_directories`，round2没有覆盖round1。
2. **PASS—参数显式传播**：`run_q3.py:159,170-174`把0.99传入M3全年模拟，未依赖隐藏的局部常量。
3. **PASS—敏感性复用正确**：`run_q3.py:177-185`只对当前主参数复用M3结果，其余四档均单独重算。
4. **PASS—追溯字段**：`run_q3.py:252-263`在指标与运行摘要中同时写入round编号和安全裕度。
5. **PASS—命令行可复现**：`run_q3.py:272-277`支持显式指定 `--round-number 2 --reserve-alpha 0.99`。
6. **PASS—结果约束**：round2年末SOC为6000 kWh，同时充放电为0，最大平衡残差为 $1.14\times10^{-13}$ kWh。
7. **PASS—结果文件**：round2已生成CSV、压缩逐时表、JSON、日志、比较图和官方工作簿。

## 阻塞与下一步

代码和round2执行均已通过。剩余阻塞是建模者尚未用自己的理由完成0.99参数决定记录，也尚未选择稳定性置信度；因此本审查不授权结果冻结。
