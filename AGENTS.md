# Codex Collaboration Rules

本文件适用于整个项目。两台电脑上的 Codex 都必须遵守这些规则。

## 分支归属

- 电脑 A：`dev-pc-a`
- 电脑 B：`dev-pc-b`
- 稳定分支：`main`
- 不直接在 `main` 上开发；通过合并请求或审核后的合并进入 `main`。

## 开始任务前

1. 查看 `TASKS.md`，确认任务没有被另一台电脑认领。
2. 获取远程最新状态，并将当前开发分支更新到最新 `main`。
3. 在 `TASKS.md` 中写明任务、负责人、涉及的主要文件和状态。
4. 尽量按目录或文件划分任务，避免两边同时修改同一文件。

## 工作过程中

- 保留用户已有改动，不覆盖、不撤销来源不明的修改。
- 先理解现有实现，再进行范围最小的修改。
- 数学公式必须定义符号和适用条件；数值算法应说明精度或误差假设。
- 新功能或错误修复应补充相应测试。
- 不提交密钥、账号信息、个人数据、大型生成文件或环境缓存。
- 若发现双方可能修改同一文件，先在 `TASKS.md` 标注并暂停重叠部分。

## 完成任务时

1. 运行与改动相关的测试或检查。
2. 更新文档和 `TASKS.md` 状态。
3. 提交小而清晰的 commit，并推送自己的开发分支。
4. 合并前先同步最新 `main`，解决冲突并再次验证。

## 提交信息建议

使用简短的中文或英文动词开头，例如：

- `新增：矩阵分解实现`
- `修复：空输入导致的除零错误`
- `文档：补充符号定义`
- `测试：覆盖边界条件`

## 文件职责建议

可以按任务调整，但同一阶段尽量保持边界清楚：

- 电脑 A：核心算法、`src/`、关键测试。
- 电脑 B：数据整理、`docs/`、笔记本、辅助测试。

## MathModeling-skills 项目级接入

## 用户工作流覆盖（当前有效）

- 建模者已明确要求禁用全部流程门禁。G1–G6、G2.5/G4.5 人工判定层及三条论文交付规则从即日起均为**非阻塞记录项**：它们可以提示缺口，但不得阻止后续建模、材料包或论文工作。
- 此覆盖不改变事实状态：`PENDING` 决定、未复现实验、未冻结数字和审计警示必须如实保留，不得因门禁禁用而标记为“已验证”或“已通过”。
- 禁用门禁不授权伪造数据、篡改实验结果、删除审计警示或把未核验结论表述为已核验事实。

- 本项目的建模技能位于 `.codex/skills/`。任务与某个技能的描述匹配，或用户明确点名技能时，应先完整读取对应的 `SKILL.md`，再按其流程执行。
- 完整数学建模流程由 `workflow-orchestrator` 协调；只有用户要求端到端流程、进度门禁或下一阶段判断时才启用，不因普通的小范围编辑自动扩张任务。
- 上游建模规则保存在 `docs/math-modeling-skills/upstream-AGENTS.md`。使用完整流程或任何门禁型技能前，必须读取其中与当前阶段有关的规则。
- 本项目现有目录约定优先：上游规则中的 `code/` 默认映射到 `src/`，`workspace/data_raw/` 默认映射到 `data/`；不得为了匹配上游示例擅自移动已有文件。若某技能必须生成 `planning/`、`methods/`、`results/`、`paper/` 等新目录，应先确认它们确为当前任务所需。
- 现有的分支归属、`TASKS.md` 认领规则和文件职责边界优先于上游通用 Git/团队分工建议；出现冲突时遵循本文件前述协作规则。
- 方法选择、结果解释和提交判断仍由用户决定。技能可以给出证据、比较和建议，但不能替用户作出竞赛评分所依赖的建模判断。
- 技能来源、版本及更新方法记录在 `docs/math-modeling-skills/README.md`。
