# 数学建模实验全记录包

本压缩包用于上传至 Git 仓库，整理日期为 2026-09-12。

## 收录内容

- `data/`：原始附件、模板、数据字典、哈希清单及数据报告。
- `planning/`：模型假设、符号表、依赖关系、进度看板和 Q1–Q4 决策日志。
- `methods/`：候选方法、PoC 代码、PoC 输出与方法池总结。
- `src/`：Q1–Q4 正式模型、公共优化与数据接口、统一运行入口及代码分析记录。
- `results/`：Q1–Q4 首轮实验报告、指标、压缩逐时明细、诊断图、运行日志和 Q3 敏感性结果。
- `output/`：五个官方 Excel 结果工作簿。
- `tests/`：方法 PoC、正式模型和正式输出的回归测试。
- `paper/`、`docs/`、`workspace/papers/`：审计材料、题目说明、文献记录和参考资料。
- `.codex/skills/`：本项目使用的建模流程规范。

## Git 状态说明

- 来源分支：`dev-pc-a`
- 打包时该分支相对 `origin/dev-pc-a` 领先 1 个本地提交。
- 已完成正式模型与首轮结果的本地提交：`7df598b`。
- `data/data_report.md` 在该提交之后还有一处未提交的状态修正；本压缩包已包含修正后的最新文件。
- 为适合上传，压缩包未包含 `.git/`；上传后由目标仓库生成新的 Git 历史。

## 复现与核验

在项目根目录按 `README.md` 安装 `requirements-model.txt` 中的依赖，然后运行：

```bash
python -m unittest discover -s tests -v
python src/run_all.py
```

上一轮完整验证为 16/16 项测试通过，统一运行状态为 `PASS`。Q3 的 `alpha=0.90` 结果保留为首轮基准，参数尚未冻结；敏感性分析显示 `alpha=0.99` 更符合尾部风险优先目标，但仍需嵌套时间验证后再更新正式结果。

## 未收录内容

- `.git/` 内部对象和本地仓库元数据
- `__pycache__/`、`.pytest_cache/`、`*.pyc`
- `tmp/` 临时渲染文件
- 虚拟环境、编辑器配置、操作系统缓存和密钥文件
